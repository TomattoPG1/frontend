import LoginPage from '@/app/auth/login/page';
import { render, screen } from '@testing-library/react';
// import LoginPage from '@/components/LoginPage';

describe('LoginPage', () => {
  it('renders login page elements', () => {
    // render(<LoginPage />);

    // // Assert that the title and description are rendered
    // expect(screen.getByText('Iniciar Sesion')).toBeInTheDocument();
    // expect(
    //   screen.getByText('Bienvenido de nuevo! Ingrese sus credenciales para acceder a su cuenta.'),
    // ).toBeInTheDocument();
    // expect(screen.getByText('Nos alegra tenerlo de vuelta.')).toBeInTheDocument();

    // // Assert that the LoginForm component is rendered
    // expect(screen.getByTestId('login-form')).toBeInTheDocument();

    expect(true).toBe(true);
  });
});
